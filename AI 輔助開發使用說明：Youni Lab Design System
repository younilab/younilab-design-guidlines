# AI 輔助開發使用指南：Youni Lab Design System

## 1. 引言

本指南旨在協助工程師利用人工智慧（AI）工具，根據 Youni Lab Design System 的規範，高效生成使用者介面（UI）。透過提供詳細的設計原則、色彩、字型、間距、元件規格及視覺預覽，AI 將能更精確地理解設計意圖，並產出符合品牌一致性的介面。本文件將說明如何準備 AI 提示（Prompt），以及建議的工作流程。

## 2. 設計系統概述

Youni Lab Design System 是一個全面的設計系統，其核心理念是「以人為本」、「自然互動」和「溫暖信任」。它透過以下三個主要文件來定義設計規範：

*   **`DESIGN.md`**：定義了基礎設計元素，如色彩、字型、間距、圓角、陰影、動畫、整體佈局和圖示系統。
*   **`COMPONENTS.md`**：詳細說明了 38 個 UI 元件的規格，包括其尺寸、狀態、變體和行為規則。
*   **`Design System 網站`**：提供所有設計元素的視覺預覽和互動範例，是理解設計系統實際應用方式的重要參考。

這些文件共同構成了一個結構化且一致的設計語言，為 AI 生成介面提供了堅實的基礎。

## 3. AI 提示（Prompt）策略

為了讓 AI 能夠精確地生成介面，有效的提示至關重要。建議採用分層次、結構化的提示方法，將設計系統的各個層面逐步引入 AI 模型。

### 3.1 基礎設計元素提示

首先，向 AI 介紹設計系統的基礎元素。這部分應包含 `DESIGN.md` 中的核心資訊。

**Prompt 範本：**

```
你是一個專業的 UI/UX 設計師，現在需要根據 Youni Lab Design System 的規範來生成介面。以下是設計系統的基礎規範：

**設計理念與原則：**
{從 DESIGN.md 複製「Philosophy — 設計理念」和「Principles — 設計原則」的內容}

**色彩系統：**
{從 DESIGN.md 複製「Colors — 色彩系統」的內容}

**字體排版：**
{從 DESIGN.md 複製「Typography — 字體排版」的內容}

**間距 / 圓角 / 邊框：**
{從 DESIGN.md 複製「Spacing / Radius / Border — 間距系統」的內容}

**陰影 / 透明度 / 動畫：**
{從 DESIGN.md 複製「Shadow / Opacity / Animation」的內容}

**佈局規範：**
{從 DESIGN.md 複製「Layout — 佈局」和「Admin Layout — 後台佈局規格」的內容}

**圖示系統：**
{從 DESIGN.md 複製「Icon System — 圖示系統」的內容}

請記住這些規範，並在後續的介面生成中嚴格遵守。
```

### 3.2 元件規格提示

在 AI 理解了基礎設計元素後，逐步引入 `COMPONENTS.md` 中的元件規格。對於每個需要使用的元件，提供其詳細的定義。

**Prompt 範本：**

```
現在，請根據以下 Youni Lab Design System 的元件規格來生成介面。我將提供你特定元件的詳細資訊。

**元件名稱：** {例如：Button — 按鈕}
**元件描述：** {從 COMPONENTS.md 複製該元件的描述}
**元件規格：** {從 COMPONENTS.md 複製該元件的詳細規格，包括尺寸、形狀、樣式、狀態、行為規則等}

請根據上述規範，生成一個 {特定元件} 的 {特定狀態/變體} 的 UI 程式碼（例如：React Component, Vue Component, HTML/CSS）。
```

### 3.3 視覺預覽與迭代提示

`Design System 網站` 提供了視覺預覽，這對於 AI 理解元件的實際呈現效果至關重要。在生成介面後，可以將網站的視覺資訊作為參考，引導 AI 進行迭代和優化。

**Prompt 範本：**

```
我已經生成了一個 {特定介面/元件} 的程式碼。請參考 Youni Lab Design System 網站上的 {特定元件} 視覺預覽（URL: {Design System 網站 URL}），檢查生成結果是否符合視覺一致性。特別注意 {例如：間距、色彩應用、圓角}。

如果存在差異，請提出修改建議並優化程式碼。
```

## 4. 工作流程建議

以下是使用 AI 輔助開發介面的建議工作流程：

1.  **初始化 AI 模型：** 首先，將 `DESIGN.md` 中的所有基礎設計元素規範輸入 AI 模型，使其建立對品牌視覺語言的全面理解。這可以作為一個「系統級」的提示，在每次會話開始時提供。

2.  **定義介面需求：** 明確需要生成的介面類型（例如：登入頁面、儀表板、表單）。

3.  **選擇所需元件：** 根據介面需求，從 `COMPONENTS.md` 中選擇將要使用的 UI 元件。

4.  **逐步生成元件：** 對於每個選定的元件，將其詳細規格（從 `COMPONENTS.md` 複製）作為提示的一部分，要求 AI 生成該元件的程式碼。例如，可以先生成一個按鈕，再生成一個輸入框。

5.  **組合與佈局：** 在單個元件生成後，提供介面的整體佈局要求（參考 `DESIGN.md` 中的佈局規範），並要求 AI 將這些元件組合起來，形成完整的介面。

6.  **視覺校驗與迭代：** 將 AI 生成的介面與 `Design System 網站` 上的視覺預覽進行對比。利用網站的 URL 作為提示的一部分，要求 AI 識別並修正視覺上的不一致之處。

7.  **程式碼優化：** 要求 AI 根據最佳實踐和特定框架（例如：React, Vue）的慣例，優化生成的程式碼。

8.  **審查與整合：** 由工程師審查 AI 生成的程式碼，進行必要的調整和整合到實際專案中。

## 5. 總結

Youni Lab Design System 提供了高度結構化和詳盡的設計規範，非常適合 AI 輔助介面生成。透過本指南提供的提示策略和工作流程，工程師可以有效地利用 AI 提升開發效率，同時確保產出的介面嚴格遵循設計系統的標準。關鍵在於提供清晰、具體的提示，並善用視覺預覽進行迭代優化。
